import { OneElementInHistoryPipe } from './one-element-in-history.pipe';

describe('OneElementInHistoryPipe', () => {
  it('create an instance', () => {
    const pipe = new OneElementInHistoryPipe();
    expect(pipe).toBeTruthy();
  });
});
